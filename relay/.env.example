# Relay Configuration

# ===== ALL VARIABLES ARE REQUIRED =====
# The relay will fail to start if any are missing

# Base application directory
# - Storage: APP_DIR/storage (for CV frames)
# - Database: APP_DIR/database/relay.db
APP_DIR=/var/lib/unblink

# CV Processing
FRAME_INTERVAL_SECONDS=5
JOB_TIMEOUT_MINUTES=5

# Ports
RELAY_PORT=9020  # WebSocket server for node and worker connections
API_PORT=8020    # HTTP API for browser clients

# Dashboard URL (for authorization flow)
DASHBOARD_URL=http://localhost:3000

# Realtime Streams
# Auto-create persistent streams for RTSP/MJPEG camera services
# Set to false to disable (CV frame extraction depends on these streams)
# Default: true
AUTO_REQUEST_REALTIME_STREAM=true

# Security
# Secret for signing JWT tokens (generate a strong random string for production)
JWT_SECRET=your-random-secret-key-here